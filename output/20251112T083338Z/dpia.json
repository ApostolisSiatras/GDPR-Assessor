{
  "activities": [
    {
      "categories_of_data": "ctx.sections.A.Governance.percent > 75 ? 'Personal data' : ''",
      "data_subjects": "ctx.org.name",
      "lawful_basis": "ctx.lawful_bases",
      "name": "DPIA",
      "purpose": "Data protection impact assessment",
      "recipients": "ctx.processors.length > 0 ? ctx.processors.map(p => p.name) : []",
      "retention": "ctx.retention",
      "security_measures": "ctx.security_measures",
      "transfers": "ctx.transfers.filter(t => t.flag !== null).map(t => t.mechanisms)"
    }
  ]
}
